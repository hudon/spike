language: python
python:
  - 2.7
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq python-numpy python-scipy
install:
  - sudo pip install pyzmq
  - sudo pip install Theano --install-option='--prefix=~/.local'
script:
  - test/unit-tests.sh
