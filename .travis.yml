language: python
python:
  - 2.7
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq python-numpy python-scipy
 - sudo apt-get install -qq python-zmq
install:
  - sudo pip install Theano --install-option='--prefix=~/.local'
script:
  - test/unit-tests.sh
